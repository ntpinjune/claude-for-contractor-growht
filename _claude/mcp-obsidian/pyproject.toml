[project]
name = "mcp-obsidian"
version = "0.1.0"
description = "Local MCP server that indexes an Obsidian vault with OpenAI embeddings"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "openai>=1.40.0",
    "sqlite-vec>=0.1.6",
    "python-dotenv>=1.0.0",
    "watchdog>=4.0.0",
]
